---
import Layout from '../layouts/Layout.astro';
import { getRoutes } from '../data/route';
import airlinesData from '../assets/airlines.json';
import csvText from '../assets/airports.csv?raw';

const airlines = airlinesData.airlines;
let allRoutes = [];

try {
  allRoutes = await getRoutes();
} catch (error) {
  console.error('Failed to load routes:', error);
  // Fallback to empty array - map will show message to select airline
}
---

<Layout>
<section class="px-6 py-8 min-h-screen">
  <div class="mb-8">
      <div class="flex justify-between items-start gap-4 mb-3">
        <h1 class="text-5xl font-bold text-[#8B1538]">Interactive Route Map</h1>
        <p class="text-sm text-gray-500 pt-2">
          <a href="/" class="text-[#8B1538] font-semibold hover:underline">Home</a> / Codeshares
        </p>
      </div>

      <p class="max-w-3xl text-lg text-gray-600 leading-relaxed">
        Explore our global network and discover where Qatari Virtual flies with our interactive route map.
      </p>
    </div>

  <!-- Controls -->
  <div class="flex justify-center items-center gap-4 mt-4">
    <select
      id="airlineSelect"
      class="border border-gray-300 px-4 py-2 rounded shadow-sm"
    >
      <option value="">Select Airline...</option>
      {airlines.map(airline => (
        <>
          <option value={airline.prefix} data-color={airline.color}>{airline.name}</option>
          {(airline.prefix === 'QR' || airline.name === 'Qatar Airways') && (
            <option value="ALL" data-color="#8B1538">All Routes</option>
          )}
        </>
      ))}
    </select>

    <select
      id="regionSelect"
      class="border border-gray-300 px-4 py-2 rounded shadow-sm"
    >
      <option value="ALL">All Regions</option>
      <option value="AF">Africa</option>
      <option value="AS">Asia & Middle East</option>
      <option value="EU">Europe</option>
      <option value="NA">North America</option>
      <option value="OC">Oceania</option>
      <option value="SA">South America</option>
    </select>
    
    <input
      id="airportSearch"
      type="text"
      placeholder="Search airport..."
      class="border border-gray-300 px-4 py-2 rounded w-60 shadow-sm"
    />

    <label class="flex items-center gap-2 cursor-pointer text-gray-700 font-medium">
      <input type="checkbox" id="showLinesToggle" class="w-4 h-4 accent-[#8B1538] cursor-pointer" />
      Show Lines
    </label>
  </div>
  
  <div id="routeInfo" class="text-center mt-2 text-sm text-gray-600"></div>

  <style>
    #map {
      height: 70vh;
      width: 100%;
      margin-top: 1rem;
    }

    .leaflet-popup-content {
      font-size: 14px;
    }

    .leaflet-popup-content strong {
      font-weight: bold;
    }
    .aircraft-icon {
      background: none;
      border: none;
      font-size: 14px;
      text-align: center;
    }
  </style>

  <div id="map"></div>

  <!-- Leaflet + Arc plugin -->
  <link
    rel="stylesheet"
    href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"
  />
  <script
    src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
    client:load
  ></script>
  <script
    src="https://unpkg.com/leaflet-arc/bin/leaflet-arc.min.js"
    client:load
  ></script>

  <script client:load define:vars={{ allRoutes, csvText, airlines }}>
    const map = L.map("map", { worldCopyJump: true }).setView([20, 10], 2);
    L.tileLayer("https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png", {
      attribution: "&copy; OpenStreetMap & CARTO",
      subdomains: "abcd",
    }).addTo(map);

    let currentColor = "#ff0000";
    let airportData = {};
    let currentRoutes = [];
    let connections = {};
    let markers = {};
    let drawn = [];
    let currentPrefix = '';
    let currentRegion = 'ALL';

    const airlineSelect = document.getElementById('airlineSelect');
    const regionSelect = document.getElementById('regionSelect');
    const showLinesToggle = document.getElementById('showLinesToggle');
    const routeInfo = document.getElementById('routeInfo');
    const searchInput = document.getElementById('airportSearch');

    function parseCSV(csvText) {
      const airports = {};
      const lines = csvText.split('\n');
      for (let i = 1; i < lines.length; i++) {
        const line = lines[i];
        if (!line) continue;
        
        // Smart CSV parser to handle commas inside quotes (e.g. "City, Country")
        let parts = [];
        let current = '';
        let inQuotes = false;
        for (let c of line) {
          if (c === '"') inQuotes = !inQuotes;
          else if (c === ',' && !inQuotes) { parts.push(current); current = ''; }
          else current += c;
        }
        parts.push(current);

        // airports.csv columns: 0:id, 1:ident, 2:type, 3:name, 4:lat, 5:lng, 6:elev, 7:continent
        if (parts.length >= 8) {
          const icao = parts[1]?.trim();
          const name = parts[3]?.trim();
          const lat = parseFloat(parts[4]);
          const lng = parseFloat(parts[5]);
          const continent = parts[7]?.trim() || 'NA'; // Missing continent usually implies NA
          
          if (icao && !isNaN(lat) && !isNaN(lng)) {
            airports[icao] = { name, icao, lat, lng, continent };
          }
        }
      }
      return airports;
    }

    function clearRoutes() {
      drawn.forEach(l => map.removeLayer(l));
      drawn = [];
    }

    function drawArc(fromCode, toCode) {
      const A = airportData[fromCode];
      const B = airportData[toCode];
      if (!A || !B) return;

      const route = currentRoutes.find(r => 
        (r.dep === fromCode && r.arr === toCode) || 
        (r.dep === toCode && r.arr === fromCode)
      );

      let routeColor = currentColor;
      if (currentPrefix === 'ALL' && route) {
        const airline = airlines.find(a => route.fltnum.startsWith(a.prefix));
        if (airline) routeColor = airline.color;
      }

      const weight = currentPrefix === 'ALL' ? 1 : 2;
      const opacity = currentPrefix === 'ALL' ? 0.5 : 1;

      // Handle crossing the Pacific (Antimeridian) by taking the shortest path
      let lngA = A.lng;
      let lngB = B.lng;
      
      if (Math.abs(lngA - lngB) > 180) {
        if (lngA > lngB) {
          lngB += 360;
        } else {
          lngB -= 360;
        }
      }

      let arc;
      try {
        arc = new L.Polyline.Arc([A.lat, lngA], [B.lat, lngB], {
          color: routeColor,
          weight: weight,
          opacity: opacity,
          vertices: 200,
        });
      } catch (e) {
        arc = L.polyline([[A.lat, lngA], [B.lat, lngB]], {
          color: routeColor, weight: weight, opacity: opacity
        });
      }

      if (route) {
        const aircraftList = route.aircraft.length > 0 ? 
          '<ul>' + route.aircraft.map(a => `<li>${a.liveryname} ${a.name}</li>`).join('') + '</ul>' : 'No aircraft assigned';
        arc.bindPopup(`<strong>${route.fltnum}</strong><br>${route.dep} → ${route.arr}<br><strong>Aircraft:</strong><br>${aircraftList}`);
      }
      
      arc.addTo(map);
      drawn.push(arc);
    }

    function highlightAirport(code) {
      if (!airportData[code]) return;

      clearRoutes();
      const connectedSet = connections[code] || new Set();

      Object.entries(markers).forEach(([otherCode, marker]) => {
        if (otherCode === code) {
          marker.setStyle({ fillOpacity: 1, opacity: 1, radius: 10 });
          marker.bringToFront();
        } else if (connectedSet.has(otherCode)) {
          marker.setStyle({ fillOpacity: 0.9, opacity: 1, radius: 8 });
        } else {
          marker.setStyle({ fillOpacity: 0.2, opacity: 0.3, radius: 6 });
        }
      });

      connectedSet.forEach(dest => drawArc(code, dest));
      const ap = airportData[code];
      map.flyTo([ap.lat, ap.lng], 4, { duration: 0.7 });
      markers[code].openPopup();
    }

    function displayRoutesForAirline(prefix) {
      currentPrefix = prefix;
      clearRoutes();
      Object.values(markers).forEach(m => map.removeLayer(m));
      markers = {};
      connections = {};

      if (!prefix) return;

      if (prefix === 'ALL') {
        currentRoutes = allRoutes;
      } else {
        currentRoutes = allRoutes.filter(route => route.fltnum.startsWith(prefix));
      }
      
      // Apply Region Filter (Inclusive: shows if either Dep or Arr is in Region)
      if (currentRegion !== 'ALL') {
        currentRoutes = currentRoutes.filter(route => {
          const depAp = airportData[route.dep];
          const arrAp = airportData[route.arr];
          const depRegion = depAp ? depAp.continent : 'OTHER';
          const arrRegion = arrAp ? arrAp.continent : 'OTHER';
          return depRegion === currentRegion || arrRegion === currentRegion;
        });
      }

      const neededICAOs = new Set();
      currentRoutes.forEach(route => {
        neededICAOs.add(route.dep);
        neededICAOs.add(route.arr);
      });

      const routeAirports = {};
      neededICAOs.forEach(icao => {
        if (airportData[icao]) {
          routeAirports[icao] = airportData[icao];
          connections[icao] = new Set();
        }
      });

      currentRoutes.forEach(route => {
        if (routeAirports[route.dep] && routeAirports[route.arr]) {
          connections[route.dep].add(route.arr);
          connections[route.arr].add(route.dep);
        }
      });

      Object.entries(routeAirports).forEach(([code, airport]) => {
        const marker = L.circleMarker([airport.lat, airport.lng], {
          radius: currentPrefix === 'ALL' ? 6 : 8,
          fillColor: currentColor,
          color: "#ffffff",
          weight: currentPrefix === 'ALL' ? 1 : 2,
          fillOpacity: 0.9,
        }).addTo(map);

        const airportRoutes = currentRoutes.filter(r => r.dep === code || r.arr === code);
        const aircraftSet = new Set();
        airportRoutes.forEach(route => {
          const otherIcao = route.dep === code ? route.arr : route.dep;
          route.aircraft.forEach(aircraft => aircraftSet.add(`${aircraft.liveryname} ${aircraft.name} - <strong>${otherIcao}</strong>`));
        });
        const aircraftList = aircraftSet.size > 0 ? 
          '<ul>' + Array.from(aircraftSet).map(a => `<li>${a}</li>`).join('') + '</ul>' : 'No aircraft';
        
        marker.bindPopup(`<strong>${airport.name}</strong><br>${airport.icao}<br><strong>Aircraft:</strong><br>${aircraftList}`);
        markers[code] = marker;
        marker.on("click", () => highlightAirport(code));
      });

      routeInfo.textContent = `${currentRoutes.length} routes, ${Object.keys(routeAirports).length} airports`;
      
      // Draw the beautiful "web" of all routes immediately if checked
      if (showLinesToggle.checked) {
        const drawnPairs = new Set();
        currentRoutes.forEach(route => {
          const pair = [route.dep, route.arr].sort().join('-');
          if (!drawnPairs.has(pair)) {
            drawArc(route.dep, route.arr);
            drawnPairs.add(pair);
          }
        });
      }

      if (Object.keys(routeAirports).length > 0) {
        const bounds = Object.values(routeAirports).map(a => [a.lat, a.lng]);
        map.fitBounds(bounds, { padding: [40, 40], maxZoom: 3 });
      }
    }

    airlineSelect.addEventListener('change', (e) => {
      currentColor = e.target.selectedOptions[0]?.dataset.color || '#ff0000';
      displayRoutesForAirline(e.target.value);
    });

    regionSelect.addEventListener('change', (e) => {
      currentRegion = e.target.value;
      displayRoutesForAirline(currentPrefix);
    });

    showLinesToggle.addEventListener('change', () => {
      displayRoutesForAirline(currentPrefix);
    });

    searchInput.addEventListener('input', () => {
      const q = searchInput.value.trim().toLowerCase();
      
      if (!q) {
        displayRoutesForAirline(currentPrefix);
        return;
      }

      const found = Object.entries(airportData).find(([code, ap]) => {
        return (
          code.toLowerCase().includes(q) ||
          ap.name.toLowerCase().includes(q) ||
          ap.icao.toLowerCase().includes(q)
        );
      });

      if (found && markers[found[0]]) {
        highlightAirport(found[0]);
      }
    });

    // Initialize
    airportData = parseCSV(csvText);
    routeInfo.textContent = 'Select an airline to view routes.';

    // Check for URL parameter to pre-select airline
    const urlParams = new URLSearchParams(window.location.search);
    const airlineParam = urlParams.get('airline');
    if (airlineParam) {
      airlineSelect.value = airlineParam;
      const selectedOption = airlineSelect.querySelector(`option[value="${airlineParam}"]`);
      if (selectedOption) {
        currentColor = selectedOption.dataset.color || '#ff0000';
        displayRoutesForAirline(airlineParam);
      }
    }
  </script>
</Layout>